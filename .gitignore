.DS_Store
.env
node_modules
node_modules_old
yarn-error.log
/cypress/screenshots/
/__sapper__/
yarn.lock

# scratch — never committed
tmp/
worker-status.json

# Fleet quality gate -- synced from ~/.claude/quality/ by sync-quality.
# Do not hand-edit or commit these; re-run sync-quality instead. baseline.json
# is deliberately NOT ignored -- it is this project's own ratchet history.
/quality/*.mjs
/quality/package.json
/quality/package-lock.json
/quality/node_modules/
